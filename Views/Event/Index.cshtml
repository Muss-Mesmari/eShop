@model eShop.Presentation.ViewModels.EventsListViewModel


<partial name="_EventsFilter" />


<h1 class="text-center text-capitalize text-nowrap">@Model.CurrentCategory</h1>
@{
    if (@Model.Events.Any())
    {
    <div class="card-deck">
        <partial name="_EventCard" />

        @*@for (int i = 0; i < @Model.Events.ToList().Count(); i++)
        {
            <partial name="_EventCard" model="@Model.Events.ToList()[i]" />
        }*@

        @*@foreach (var item in Model.Events)
        {
            <partial name="_EventCard" model="item" />
        }*@

    </div>
    }
    else
    {
        <div class="container-fluid">
            <p class="text-danger text-center"> @Model.NotFoundSearchedEventMessage</p>
        </div>
    }
}


